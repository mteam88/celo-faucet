{
    "name": "celo-faucet-web",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "esbuild main.ts --bundle --outfile=dist/main.js --minify",
        "dev": "esbuild main.ts --bundle --outfile=dist/main.js --watch"
    },
    "devDependencies": {
        "esbuild": "^0.19.0"
    }
}